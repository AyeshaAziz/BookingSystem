import { GlobalConstants } from '../../GlobalConstants.helper';

const paperStyleProps = {
    padding: 2,
    margin: 2,
    minWidth: 120,
    backgroundColor: 'white',
    width: 350,
  };

  const formControlStyleProps = {
    m: GlobalConstants.ONE,
    minWidth: 300,
  };

  const buttonStyleProps = { m: GlobalConstants.ONE, background: '#1979a9' };

  export {paperStyleProps, formControlStyleProps, buttonStyleProps}